# 🚀 Quick Start - BERT Integration

## ✅ Что сделано

В твой проект интегрирована **BERT модель для анализа лояльности** от друга:

1. ✅ **Модуль `src/bert_loyalty.py`** - работа с BERT моделью
2. ✅ **Обновлён `app.py`** - новая вкладка "🧠 BERT Анализ"
3. ✅ **Сравнение методов** - BERT vs keyword
4. ✅ **Относительный анализ** - z-score, байесовский балл (как у друга)
5. ✅ **Визуализации** - интерактивные графики

## 🎯 Как запустить

### 1. Проверь наличие BERT модели

```bash
ls Golden-apple-loyalty/models_binary_fixed_v2/
```

Должны быть файлы: `config.json`, `pytorch_model.bin`, `tokenizer_config.json`, и др.

### 2. Запусти Streamlit

```bash
streamlit run app.py
```

### 3. Активируй BERT в sidebar

1. Прокрути sidebar вниз до **"🤖 BERT Анализ"**
2. Поставь галочку **"🧠 Запустить BERT анализ лояльности"**
3. Подожди 1-5 минут (зависит от размера данных)
4. Появится новая вкладка **"🧠 BERT Анализ"**

## 📊 Что доступно в BERT вкладке

### 1. Общая статистика
- Количество лояльных по 3 порогам (строгий 0.718, средний 0.55, мягкий 0.40)
- Распределение вероятностей BERT

### 2. Сравнение с keyword методом
- Scatter plot: keyword score vs BERT probability
- Корреляция между методами
- Диссонансы (расхождения)

### 3. Относительный анализ продуктов (как у друга!)
- Настрой минимум отзывов (слайдер)
- Нажми **"🔄 Рассчитать относительные показатели"**
- Получишь:
  - **Z-score** для каждого продукта
  - **Топ-10** по относительной лояльности
  - **Проблемные** продукты
  - **Категоризацию**: 🚀 Выдающийся, 📈 Выше среднего, 📊 Средний, ⚠️ Ниже среднего, 🔥 Проблемный
  - **Графики** распределения z-scores
  - **Экспорт** в CSV

## 🔑 Ключевые метрики

### Z-score (относительная лояльность)
- `z > 2` → 🚀 Выдающийся
- `z ≥ 1` → 📈 Выше среднего
- `-1 < z < 1` → 📊 Средний
- `z ≤ -1` → ⚠️ Ниже среднего
- `z < -2` → 🔥 Проблемный

### BERT вероятность
- `≥ 0.718` → Лояльный (строгий порог)
- `≥ 0.55` → Лояльный (средний порог)
- `≥ 0.40` → Лояльный (мягкий порог)

## 📝 Примеры использования

### Найти лучшие продукты
1. Перейди на вкладку **"🧠 BERT Анализ"**
2. Прокрути до **"🏆 Относительный анализ продуктов"**
3. Настрой минимум отзывов (например, 100)
4. Нажми **"🔄 Рассчитать"**
5. Смотри **"🏆 Топ-10 по относительной лояльности"**

### Найти проблемные продукты
- В той же секции смотри **"⚠️ Проблемные продукты"**
- Это продукты с низким z-score (значительно ниже среднего)

### Сравнить методы
- В разделе **"🔬 Сравнение: BERT vs Keyword метод"**
- Смотри scatter plot и корреляцию
- Диссонансы показывают где методы не согласны

### Экспорт результатов
- Внизу секции относительного анализа нажми **"Скачать относительный анализ продуктов (CSV)"**
- Получишь CSV с z-score, байесовскими баллами, категориями для всех продуктов

## ⚡ Производительность

- **CPU**: ~50-100 отзывов/сек
- **GPU**: ~500-1000 отзывов/сек
- **Кэширование**: Результаты сохраняются в session_state (не нужно перезапускать)

## 🐛 Troubleshooting

### Модель не загружается
```
❌ BERT модель недоступна
```
**Решение**: Проверь путь `Golden-apple-loyalty/models_binary_fixed_v2/`

### Медленно работает
**Решение**:
- Это нормально для BERT на CPU
- Первый запуск всегда медленный (загрузка модели)
- Повторные запуски быстрее (кэширование)

### Ошибка памяти
**Решение**: Уменьши batch_size в коде `bert_loyalty.py` (строка с `batch_size=32`)

## 📚 Документация

Полная документация: [BERT_INTEGRATION.md](BERT_INTEGRATION.md)

## 🎉 Готово!

Теперь у тебя есть:
- ✅ Два метода анализа лояльности (keyword + BERT)
- ✅ Сравнение методов
- ✅ Относительный анализ продуктов (как у друга)
- ✅ Интерактивные визуализации
- ✅ Экспорт результатов

**Запускай и пользуйся!** 🚀
